name: SonarQube Analysis

on:
  push:
    branches: [ "dev", "main" ]
  pull_request:
    branches: [ "dev", "main" ]

jobs:
  call-sonar-fullstack:
    uses: IFPBEsp/APAE-INFRA/.github/workflows/reusable-sonarqube-fullstack.yml@dev
    with:
      backend-project-key: "apae-gestao-escolar-backend"
      frontend-project-key: "apae-gestao-escolar-frontend"
      backend-dir: "api"
      frontend-dir: "app"
      organization: "ifpbesp"
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}